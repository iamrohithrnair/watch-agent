# Scripts

Project scripts are kept here when they are not tied to a single package. Environment validation currently lives in `packages/core/src/scripts/check-env.ts` so it can reuse the shared config loader.
